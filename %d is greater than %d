#include<stdio.h>
int main()
{
	int x = 3;
	int y = 5;

		if (x > y)
		{
			printf("%d is greater than %d", x, y);
		}
	    
	if (y > x)
	{
		printf("%d is greater than %d", y,x );
	}
	getchar();

	return 0;
}
